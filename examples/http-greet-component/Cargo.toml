[package]
name = "http-greet-component"
version = "0.5.0"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
wit-bindgen = { version = "0.60.0", default-features = false, features = ["realloc", "macros"] }

[profile.release]
opt-level = "s"
strip = true
