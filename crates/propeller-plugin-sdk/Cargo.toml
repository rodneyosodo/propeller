[package]
name = "propeller-plugin-sdk"
version = "0.1.0"
edition = "2021"
description = "SDK for writing Propeller manager plugins in Rust/WASM"
license = "Apache-2.0"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
